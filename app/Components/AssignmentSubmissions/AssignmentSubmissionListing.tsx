import type { AssignmentSubmission } from "~/types/assignment";

export default function AssignmentSubmissionListing({ assignmentSubmission }: { assignmentSubmission: AssignmentSubmission}) {
    return (
        <div></div>
    );
}